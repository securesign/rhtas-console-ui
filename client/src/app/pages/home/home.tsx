import type React from "react";

import { PageSection } from "@patternfly/react-core";

export const Home: React.FC = () => {
  return <PageSection hasBodyWrapper={false}>hello world</PageSection>;
};
